package com.importre.example.main.anim

import android.graphics.Color

data class AnimModel(
    val checked: Boolean = false,
    val bgColor: Int = Color.BLACK,
    val toolbarColor: Int = Color.BLACK
)
